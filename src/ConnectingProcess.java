//to store the address & port of the connections to other processes
public class ConnectingProcess {

    String IP;  //IP Address
    int port; //connecting port

    //constructor
    public ConnectingProcess(String IP, int port) {
        this.IP = IP;
        this.port = port;
    }
}
